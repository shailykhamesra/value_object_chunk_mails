<!DOCTYPE html>
<html>
  <body>
    <h1>Contacts</h1>
    <table>
      <tr>
        <th>Id</th>
        <th>Name</th>
        <th>Email</th>
        <th colspan="3"></th>
      </tr>
      <% @contacts.each do |contact| %>
        <tr>
          <td><%= contact.id %></td>
          <td><%= contact.name_exists %></td>
          <td><%= contact.email %></td>
        </tr>
      <% end %>
    </table>
  </body>
</html>
